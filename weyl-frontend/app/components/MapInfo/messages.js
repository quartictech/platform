/*
 * MapInfo Messages
 *
 * This contains all the text for the MapInfo component.
 */
import { defineMessages } from "react-intl";

export default defineMessages({
  header: {
    id: "app.components.MapInfo.header",
    defaultMessage: "This is the MapInfo component !",
  },
});
