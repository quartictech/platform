/*
 * GeofenceSettings Messages
 *
 * This contains all the text for the GeofenceSettings component.
 */
import { defineMessages } from "react-intl";

export default defineMessages({
  header: {
    id: "app.components.GeofenceSettings.header",
    defaultMessage: "This is the GeofenceSettings component !",
  },
});
