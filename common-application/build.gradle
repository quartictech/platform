plugins {
    id "java"
    id "idea"
    id "org.inferred.processors" version "1.2.3"
}

dependencies {
    processor libraries.immutables

    compile project(":common-core")
    compile "io.dropwizard:dropwizard-core:${versions.dropwizard}"
    compile "io.dropwizard.modules:dropwizard-java8:0.9.0-1"
    compile "com.netflix.feign:feign-core:${versions.feign}"
    compile("com.netflix.feign:feign-jaxrs:${versions.feign}") {
        exclude group: "javax.ws.rs", module: "jsr311-api"      // This is JAX-RS 1.0, which conflicts with Jersey reliance on 2.0
    }
    compile "com.netflix.feign:feign-jackson:${versions.feign}"
    compile "com.netflix.feign:feign-slf4j:${versions.feign}"

    testCompile libraries.testCommon
}


