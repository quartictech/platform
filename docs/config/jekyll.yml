# Jekyll config

source: "src"

destination: "build/bundle"

exclude:
  - "assets"

include:
  - "_pages"

keep_files:
  - "assets"

incremental: true

defaults:
  - scope:
      path: ""
    values:
      layout: "default"

markdown: kramdown