package io.quartic.common.test

/**
 * Used to denote a test that we might one day split out into a separate "integration tests" Gradle task.
 */
@Target(AnnotationTarget.CLASS)
annotation class IntegrationTest
