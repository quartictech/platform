localDisk: true

gcs:
  bucketSuffix: test.quartic.io

server:
  applicationContextPath: /
  applicationConnectors:
    - type: http
      port: 8120
  adminConnectors:
    - type: http
      port: 8121
