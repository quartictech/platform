kubernetes:
  enable: false
  namespace: qube
  numConcurrentJobs: 4
  jobTimeoutSeconds: 3600
  podTemplate:
    spec:
      automountServiceAccountToken: false
      containers:
        - name: container
      restartPolicy: Never

database:
  user: postgres
  port: 15432
  databaseName: qube

websocketPort: 8202
url:
  port: 8200
